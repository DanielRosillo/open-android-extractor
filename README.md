# open-android-extractor
 OpenAndroidExtractor(OAE) is a desktop-based tool designed to extract and backup data from Android devices using ADB. It provides a clean and intuitive interface to perform structured data acquisition without requiring root access.
